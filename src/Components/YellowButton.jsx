

const YellowButton = ({text}) => {
    return (
        <button className="btn btn-md btn-warning uppercase rounded-none font-semibold text-black">
        {text}
        </button>
    );
};

export default YellowButton;